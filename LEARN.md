# 🎓 学習ガイド：今日のノートの仕組み (Learning Guide)

このドキュメントでは、『今日のノート（DeskNote）』がどのような仕組みで動いているかを、専門用語をできるだけ避けて解説します。

---

## 🗂️ 1. 全体像

このアプリは、次の4つのファイルでできています。ビルド（変換作業）は一切ありません。書いたものがそのままブラウザで動きます。

1. `public/index.html` — 画面に並ぶ部品（入り口・タブ・一覧・入力欄）
2. `public/style.css` — 見た目
3. `public/app.js` — 予定やToDoを覚えて、並べ替えて、共有する処理
4. `public/demo.js` — お試し用のサンプルデータ

外部から読み込んでいるのは **Firebase**（グーグルのデータ置き場）だけで、CDN からそのまま読み込んでいます。

---

## 🔑 2. ノートIDという鍵

このアプリにはログインがありません。代わりに「ノートID」という鍵が1本あるだけです。

```js
function newRoomId() {
    const alphabet = 'abcdefghijkmnpqrstuvwxyz23456789';
    const bytes = crypto.getRandomValues(new Uint8Array(20));
    return [...bytes].map(b => alphabet[b % alphabet.length]).join('');
}
```

*   `crypto.getRandomValues` は、予測のできない乱数を作る仕組みです
*   使う文字から `l` `o` `1` `0` を外してあります。読み間違えて伝わらないようにするためです
*   20文字あるので、総当たりで他人のノートにたどり着くことは現実的にできません

そのIDが、そのままデータの置き場所（`rooms/{ノートID}`）の名前になります。IDを知っている＝ノートを開ける、という単純な作りです。

**裏を返すと、IDを知っている人は誰でも中身を読み書きできます。** パスワードなど、人に見られて困るものを書いてはいけないのはこのためです。

---

## 🔄 3. みんなの画面が同時に変わるしくみ

共有の中心にあるのが `onSnapshot` という関数です。

```js
unsubscribe = onSnapshot(ref, snap => { ... });
```

これは「このノートに変化があったら教えて」とお願いしておく仕組みです。誰かが予定を1つ足すと、その知らせが同じノートを開いている全員に届き、それぞれの画面が描き直されます。**自分から確認しに行くのではなく、向こうから届く**のがポイントです。

書き込むときは `setDoc(..., { merge: true })` を使っています。`merge` を付けると、送った部分だけが差し替わり、他の人が同時に書いた部分を消してしまわずに済みます。

---

## 📴 4. 電波がなくても書ける理由

```js
db = initializeFirestore(firebaseApp, { localCache: persistentLocalCache() });
```

この1行で、Firestore が端末の中にも同じ内容の控えを持つようになります。

*   書いた内容はまず端末の控えに入るので、画面はすぐ変わります
*   電波が切れている間の変更は端末にたまり、つながった瞬間にまとめて送られます
*   アプリを開いた直後も、通信を待たずに前回の内容がすぐ表示されます

---

## 👤 5. ホストと参加者

ノートを最初に作った人が「ホスト」です。その人の見る人ID（`viewerId`）が、ノートの `hostId` として保存されます。

*   ホストだけが、ノートに正式な名前を付けられます
*   ホストだけが、参加者を退出させられます（`bannedViewers` に入れると、その人の画面はノートから閉じます）
*   参加している人には、端末ごとに顔（絵文字）が割り当てられ、いま誰が見ているかが分かります

---

## 🛡️ 6. Firestore のルール

`firestore.rules` が、誰がどこを読み書きできるかを決めています。

```
match /rooms/{roomId} {
  allow read, write: if roomId.size() >= 16 && roomId.size() <= 64;
}
```

ログインを要求しない代わりに、「十分に長いIDを知っていること」を通行証にしています。短いIDを総当たりで試して他人のノートを開く、という攻撃を防ぐための下限が `16` です。

---

## 📅 7. 4つの見方

同じ予定の一覧を、日付で切り分けて見せているだけです。

| タブ | 出しているもの |
|---|---|
| あした | 明日の日付の予定 |
| これから | 明日より後の予定 |
| これまで | 今日より前、または日付のない予定 |
| カレンダー | 月の表に、予定のある日を印として並べたもの |

並べ替えは、これからの予定は日付の早い順、過去は新しい日から、という具合に、見たい順番が変わることに合わせて切り替えています。

---

## 💻 8. 手元で動かす

```bash
npx serve public
```

Firestore は公開中のプロジェクトにつながっています。自分だけの環境で試したい場合は、`public/app.js` の先頭にある Firebase の設定を、ご自身のプロジェクトのものに置き換えてください。
